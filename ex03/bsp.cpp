#include "Point.hpp"

static bool is_one_sided( Point const line_p_one, Point const line_p_two, Point const point_one, Point const point_two) {

}

bool bsp( Point const a, Point const b, Point const c, Point const point) {
	(void)a;
	(void)b;
	(void)c;
	(void)point;
	return(true);
}